rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEVELOPER MODE: Open for all reads and writes
    // Change this before going to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
